# 🔍 ขั้นตอนการทดสอบหลังแก้ไข Frontend

## สิ่งที่แก้ไขไปแล้ว

✅ เปลี่ยน API call จาก GET (query parameter) เป็น POST (request body)

## วิธีทดสอบ

### 1. Hard Refresh Browser
กด **Ctrl + Shift + R** (Windows) หรือ **Cmd + Shift + R** (Mac) เพื่อ clear cache และ reload

### 2. ตรวจสอบ Network Tab
1. เปิด DevTools (F12)
2. ไปที่ tab "Network"
3. Refresh หน้ากระเป๋า
4. **ดูว่ามี request ไปยัง `get-my-coupons` หรือไม่**
5. ถ้ามี คลิกที่ request นั้นแล้วดู:
   - **Headers** → Request Method ต้องเป็น `POST`
   - **Payload** → ต้องมี `{"userId":"Ua636ab14081b483636896549d2026398"}`
   - **Response** → ต้องมีข้อมูลคูปอง 4 อัน

### 3. ตรวจสอบ Console
ดูว่ามี error อะไรหรือไม่ใน Console tab

## ถ้ายังไม่เห็นคูปอง

ส่ง screenshot ของ:
1. Network tab (แสดง request ไปยัง get-my-coupons)
2. Console tab (แสดง error ถ้ามี)
