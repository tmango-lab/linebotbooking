# 📱 วิธีสมัครและตั้งค่า LIFF ID

เพื่อให้ปุ่ม **"เก็บเข้า Wallet"** ทำงานได้ เราต้องสร้าง **LIFF ID** ก่อนครับ โดยมีขั้นตอนดังนี้:

## 1. เข้าสู่ระบบ LINE Developers
1. ไปที่ [LINE Developers Console](https://developers.line.biz/console/)
2. กดปุ่ม **Log in** (ใช้ LINE ส่วนตัวได้เลย)

## 2. สร้าง Channel (LINE Login)
*แนะนำให้แยก Channel สำหรับ Web App (Wallet) ออกจาก Bot*

1. เลือก **Provider** ของเรา (ถ้ายังไม่มีให้กด Create New Provider)
2. กด **Create a new channel**
3. เลือกประเภท **LINE Login** (โลโก้กุญแจสีเขียว)
4. กรอกข้อมูล:
   - **Region**: Thailand
   - **Channel Name**: ชื่อแอพ เช่น "My Booking Wallet"
   - **Channel Description**: รายละเอียดสั้นๆ
   - **App Type**: Web app
5. ติ๊กถูกยืนยันข้อตกลง แล้วกด **Create**

## 3. สร้าง LIFF App (เพื่อเอา ID)
1. เมื่อสร้าง Channel เสร็จ ให้กดเข้าไปที่ Channel นั้น
2. ไปที่แท็บ **LIFF** (ด้านบน)
3. กดปุ่ม **Add**
4. กรอกข้อมูลการตั้งค่า:
   - **LIFF app name**: เช่น "Wallet"
   - **Size**: แนะนำ **Tall** หรือ **Full**
   - **Endpoint URL**: 
     * *คือลิ้งค์ Web Frontend ของเรา*
     * ⚠️ **ถ้ายังไม่มีเว็บจริง:** ให้ใส่ `https://google.com` ไปก่อนเพื่อขอ ID
     * ⚠️ **ถ้าจะเทสจริง:** ต้องใส่ URL ของหน้าเว็บ Wallet ที่รันอยู่ (เช่น `https://my-app.vercel.app` หรือถ้า Local ต้องใช้ ngrok เป็น `https://xxxx.ngrok-free.app`)
   - **Scopes**: ติ๊ก `profile` และ `openid`
   - **Bot link feature**: เลือก `On (Normal)` หรือ `Off` ก็ได้
5. กด **Add**

## 4. รับ LIFF ID 🎉
เมื่อกด Add เสร็จแล้ว จะเห็นรหัส **LIFF ID** ปรากฏอยู่ (รูปแบบ `1657xxxxxx-xxxxxx`)

👉 **ให้นำเลข LIFF ID นี้มาบอกผมได้เลยครับ!**
ผมจะนำไปใส่ในโค้ด เพื่อให้ปุ่ม "เก็บเข้า Wallet" วิ่งไปที่หน้าเว็บของเราได้อย่างถูกต้องครับ
