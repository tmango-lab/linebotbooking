# 📋 คู่มือทดสอบ Wallet API แบบละเอียด

## ขั้นตอนที่ 1: เปิดไฟล์ HTML Test

### วิธีที่ 1: เปิดจาก File Explorer (แนะนำ)
1. เปิด **File Explorer** (กด Windows + E)
2. ไปที่โฟลเดอร์: `C:\Users\Tmango\Desktop\ระบบจองสนาม`
3. หาไฟล์ชื่อ: **`test_wallet_api_debug.html`**
4. **ดับเบิลคลิก** ที่ไฟล์นั้น
5. Browser จะเปิดขึ้นมาอัตโนมัติ

### วิธีที่ 2: เปิดจาก VS Code
1. ใน VS Code, เปิดไฟล์ `test_wallet_api_debug.html`
2. **คลิกขวา** ที่ไฟล์
3. เลือก **"Open with Live Server"** (ถ้ามี extension)
   - หรือเลือก **"Reveal in File Explorer"** แล้วดับเบิลคลิกไฟล์

### วิธีที่ 3: Copy Path แล้ววางใน Browser
1. ใน VS Code, **คลิกขวา** ที่ไฟล์ `test_wallet_api_debug.html`
2. เลือก **"Copy Path"**
3. เปิด **Chrome** หรือ **Edge**
4. วางที่ Address Bar (Ctrl + V)
5. กด Enter

---

## ขั้นตอนที่ 2: ทดสอบ API

เมื่อหน้าเว็บเปิดขึ้นมา คุณจะเห็น:

```
🔍 Debug: get-my-coupons API

User ID: [Ua636ab14081b483636896549d2026398]

[🧪 Test API]  [🗑️ Clear]

Output:
Ready! Click "Test API" to start.
```

### ทำตามนี้:
1. ✅ **ตรวจสอบ User ID** ว่าถูกต้องหรือไม่
   - ถ้าไม่ใช่ User ID ของคุณ ให้แก้ไขในช่อง input
   
2. ✅ **กดปุ่ม "🧪 Test API"**

3. ✅ **รอสักครู่** (ประมาณ 2-3 วินาที)

4. ✅ **อ่านผลลัพธ์** ใน Output box

---

## ขั้นตอนที่ 3: ส่งผลลัพธ์ให้ผม

### สิ่งที่ต้องส่ง:

#### A. Screenshot จากหน้า HTML Test
กด **Windows + Shift + S** แล้วจับภาพหน้าจอส่วน Output ทั้งหมด

**ดูว่ามีข้อความอะไร:**
- ✅ ถ้าเห็น `"Total: 4"` หรือมากกว่า 0 = **API ทำงานถูกต้อง**
- ❌ ถ้าเห็น `"Total: 0"` = **คูปองถูก filter ออก**
- ❌ ถ้าเห็น `"API Error"` = **มีปัญหาที่ API**

---

## ขั้นตอนที่ 4: รัน SQL Query

### เปิด Supabase Dashboard:
1. เปิด Browser ไปที่: https://supabase.com/dashboard
2. Login เข้าบัญชีของคุณ
3. เลือก Project: **kyprnvazjyilthdzhqxh**
4. ไปที่เมนู **"SQL Editor"** (ด้านซ้าย)

### รัน Query:
1. คลิก **"New Query"**
2. เปิดไฟล์ `debug_wallet_issue.sql` ใน VS Code
3. **Copy ทั้งหมด** (Ctrl + A, Ctrl + C)
4. **Paste** ใน SQL Editor (Ctrl + V)
5. กดปุ่ม **"Run"** (หรือกด Ctrl + Enter)

### ส่งผลลัพธ์:
- จับภาพหน้าจอ **ทั้ง 3 ตาราง** ที่แสดงผลลัพธ์
- หรือ Copy ข้อมูลในตารางส่งมาเป็น text

---

## ตัวอย่างผลลัพธ์ที่คาดหวัง

### ✅ กรณีที่ถูกต้อง (API ควรแสดงคูปอง):
```
📦 Response Body:
{
  "success": true,
  "main": [
    {
      "coupon_id": "abc123...",
      "name": "ส่วนลดพิเศษ",
      "expiry": "2026-02-15T10:00:00Z"
    }
  ],
  "on_top": [],
  "total": 1
}

✅ API Success!
   Main Coupons: 1
   On-Top Coupons: 0
   Total: 1
```

### ❌ กรณีที่มีปัญหา (คูปองไม่แสดง):
```
📦 Response Body:
{
  "success": true,
  "main": [],
  "on_top": [],
  "total": 0
}

⚠️ No coupons returned!
   Possible reasons:
   1. expires_at is NULL
   2. expires_at is in the past
   3. status is not ACTIVE
```

---

## ถ้าติดปัญหา

### ปัญหา: ไฟล์ HTML ไม่เปิด
**แก้ไข**: 
- ตรวจสอบว่าไฟล์อยู่ที่ `C:\Users\Tmango\Desktop\ระบบจองสนาม\test_wallet_api_debug.html`
- ลอง copy path แล้ววางใน browser โดยตรง

### ปัญหา: กดปุ่ม Test แล้วไม่มีอะไรเกิดขึ้น
**แก้ไข**:
1. กด F12 เปิด DevTools
2. ไปที่ tab "Console"
3. ดูว่ามี error อะไรหรือไม่
4. ส่ง screenshot Console มาให้ผมดู

### ปัญหา: SQL Query รันไม่ได้
**แก้ไข**:
- ตรวจสอบว่า login Supabase แล้ว
- ตรวจสอบว่าเลือก Project ถูกต้อง
- ลอง copy query ทีละส่วน (แยก 3 query)

---

## สรุป: ส่งอะไรมาให้ผม

1. ✅ **Screenshot** จากหน้า HTML Test (ส่วน Output)
2. ✅ **Screenshot** จาก SQL Query Results (ทั้ง 3 ตาราง)
3. ✅ (ถ้ามี error) Screenshot จาก Console (F12)

ส่งมาให้ผมดูแล้วจะแก้ไขต่อได้เลยครับ! 🚀
